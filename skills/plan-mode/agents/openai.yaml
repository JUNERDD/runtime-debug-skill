interface:
  display_name: "Plan Mode"
  short_description: "Create editable implementation plans"
  default_prompt: "Use $plan-mode to create or update an editable Markdown plan with code references and todos, validate it, and preserve the requested planning or implementation scope."
