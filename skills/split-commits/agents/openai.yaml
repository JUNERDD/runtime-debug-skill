interface:
  display_name: "split-commits"
  short_description: "Split large changes into focused commits"
  default_prompt: "Use $split-commits to split the current working tree into focused local commits, honoring the authorized batches and any requested per-commit confirmation."
