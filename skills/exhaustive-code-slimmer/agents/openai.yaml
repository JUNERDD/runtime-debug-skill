interface:
  display_name: "Exhaustive Code Slimmer"
  short_description: "Maximize safe code deletion with DX-aware architecture gating"
  default_prompt: "Use $exhaustive-code-slimmer to exhaustively shrink this codebase while preserving behavior and readability, carrying forward approved architecture scope and asking only for unresolved boundary decisions."

policy:
  allow_implicit_invocation: false
